# 数据完整性红线：禁止静默截断（最高优先级）

## 核心原则

**任何形式的数据截断（行数、字符数、页数、批次大小）若未经用户明确授权、未在代码中显著标注警告，均属严重违规。**

此问题已多次造成生产数据损失，包括：
- 邮件采集中静默截断导致邮件遗漏
- 知识库采集中限制行数导致文档内容不全
- 受试者礼金档案导入中 `max_row=210` 截断，导致超过 1,800 万元礼金数据丢失

## 绝对禁止（不得以任何理由绕过）

- ❌ **禁止在文件/数据库读取中硬编码行数上限**（如 `max_row=200`、`head(1000)`、`[:500]` 等），除非用户明确要求限制
- ❌ **禁止在调试用的 `--limit` 参数默认值设为非零**（默认必须 = 0，即不限制）
- ❌ **禁止对 API 分页采集中静默停止翻页**（必须翻到最后一页或用户指定上限）
- ❌ **禁止在日志或报告中隐藏"被跳过/被截断"的数量**（必须明确输出）
- ❌ **禁止以"性能优化"为由截断数据**，性能优化只能通过分批写入、并行处理等方式，不能减少读取数据量

## 必须遵守

- ✅ 读取 Excel/CSV/文本文件时，**必须读取全部内容**（`max_row=None`，无上限）
- ✅ 若文件确实太大需要分片，**必须用进度日志记录"已读取 N 行，文件共 M 行"**
- ✅ 调试/测试参数（如 `--limit-files`、`--dry-run`）**默认值必须为 0 或 False**
- ✅ 任何批处理脚本，**完成后必须输出"总计读取 X 条，写入 Y 条，跳过 Z 条"的对账摘要**
- ✅ "跳过"的记录必须说明原因（格式不匹配、重复、无效等），不能静默丢弃

## 代码审查检查项

在审查或生成以下代码时，**必须主动检查是否存在截断**：

```
# 危险模式，发现即报警：
ws.iter_rows(max_row=XXX)     ← 必须改为 max_row=None
df.head(N)                    ← 调试代码，不得进入生产
queryset[:N]                  ← 是否遗漏后续数据？
LIMIT N                       ← SQL 中非分页用途的 LIMIT
result[:N]                    ← 切片截断
break / continue              ← 循环中的提前退出，是否有遗漏？
```

## 历史教训（真实案例）

| 时间 | 场景 | 截断位置 | 数据损失 |
|------|------|----------|----------|
| 2026-03 | 受试者礼金档案导入 | `max_row=HEADER_SEARCH+200`（共210行/Sheet） | 每个Sheet超过200行的记录全部丢失，估算1800-2900万元礼金数据 |
| 历史 | 飞书邮件采集 | API 分页静默停止 | 邮件不完整，导致信息上下文缺失 |
| 历史 | 知识库文档采集 | 文档截取前N字 | 文档后半段内容丢失，影响知识检索质量 |

## AI 的职责

当 AI 生成任何涉及数据读取/采集/导入的代码时，**必须主动说明**：
1. 代码是否读取了全量数据（无截断）
2. 如果有大小限制，必须说明原因并征得用户同意
3. 完成后的对账数字（读了多少、写了多少、跳过多少）必须输出
