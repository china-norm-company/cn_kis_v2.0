from django.apps import AppConfig


class ProductDistributionConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.product_distribution'
    verbose_name = '样品发放（产品发放）'
