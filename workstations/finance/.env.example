# 复制为 .env.development.local 按需覆盖（已 gitignore）
#
# 【必填 / 强烈建议】须与 backend/.env 的 FEISHU_APP_ID（及飞书「网页」授权用的应用）一致。
# 未设置时开发环境会回退到 feishu-sdk 的 PRIMARY_APP_ID_FALLBACK（与 backend/.env.example 对齐）；
# 若你改过后端 FEISHU_APP_ID 却未在此配置，会出现「浏览器用 App A 授权、后端只有 App B 的 Secret」→ 飞书 20024。
# VITE_FEISHU_APP_ID=cli_a907f21f0723dbce
#
# 仍报 20027 时：错误页可能只列部分 scope。SDK 默认还会要 im/mail/task/drive/contact 等（见 packages/feishu-sdk/src/auth.ts）。
# 请在该飞书应用中逐项开通；或临时用下面一行收窄授权范围做排查（仅开发，空格分隔）：
# VITE_FEISHU_OAUTH_SCOPES=offline_access contact:user.base:readonly contact:user.email:readonly
#
# 飞书开放平台「安全设置 → 重定向 URL」须与本机访问地址完全一致（含协议、主机、端口、末尾 /）：
#   http://localhost:3004/finance/
# 勿同时开多个 Vite 占满 3004；本仓库已设 strictPort，端口被占用时会直接报错而非自动换端口。
#
# ── 飞书 OAuth 走「已部署服务器」中转（解决本机命中 20027 权限不足：开放平台在服务器应用上已开通 scope）
# 填写与线上一致的 API 根，例如：https://your-prod.example.com/api/v1
# VITE_FEISHU_OAUTH_RELAY_API_BASE=https://your-prod.example.com/api/v1
# 注意：JWT 由部署端 SECRET_KEY 签发。使用中转登录后，请将业务 API 也指向同一部署环境，例如：
# VITE_API_MODE=real
# VITE_API_BASE_URL=https://your-prod.example.com/api/v1
# 否则 token 在本机 8001 上无法通过 JWT 校验。部署端 CORS 需包含 http://localhost:3004。
