# 复制为 .env 后按需修改；构建前 Taro 会读取（见 config/index.ts）
#
# ========== 本地联调（与物料台同逻辑，需本机跑 Django）==========
# 1) 仓库根目录执行:  powershell -File scripts/dev-backend-mini.ps1
#    （已绑定 0.0.0.0:8001，局域网可访问）
# 2) 微信开发者工具：详情 -> 本地设置 -> 勾选「不校验合法域名、web-view」
# 3) 修改下方 TARO_APP_API_BASE 后重新构建小程序
#
# --- 仅开发者工具模拟器：可用 127.0.0.1 ---
# TARO_APP_API_BASE=http://127.0.0.1:8001/api/v1
#
# --- 手机真机预览：不要用 127.0.0.1 ---
# 真机上的 127.0.0.1 指向手机自己，连不到你电脑。
# 请在本机执行 ipconfig / ifconfig，将 IPv4 填成（示例）：
# TARO_APP_API_BASE=http://192.168.1.100:8001/api/v1
# 手机与电脑同一 WiFi，防火墙放行 8001，改 .env 后必须重新构建。
#
TARO_APP_API_BASE=http://127.0.0.1:8001/api/v1
#
# ========== 与工作台联通（生产/测试 HTTPS，与物料台同库）==========
# 将 API 指到与浏览器访问物料台相同的后端，例如：
# TARO_APP_API_BASE=https://your-domain.com/api/v1
# 若希望在已配置绝对 API 基址时强制跳过云托管优先逻辑，可设：
# TARO_APP_USE_DIRECT_API=true
#
# 微信公众平台需配置 request 合法域名（仅域名，无路径）
