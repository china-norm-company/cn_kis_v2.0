# 器衡·设备台前端环境变量示例（复制为 .env 后使用）
#
# ⚠️ 重定向 URL 必须加在「授权时使用的那个飞书应用」里（与下方 VITE_FEISHU_APP_ID 一致）：
# V2 默认 VITE_FEISHU_APP_ID=cli_a98b0babd020500e（子衿主应用），则请到子衿应用 → 安全设置 → 重定向 URL
# 添加 http://localhost:3010/equipment/（含尾斜杠）。仅加在「仪器设备」等其它应用下不会生效，仍会报 20029。
VITE_FEISHU_APP_ID=cli_a98b0babd020500e
VITE_API_BASE_URL=/api/v1
# 本地固定授权回跳源，避免用 127.0.0.1 或 Vite 备用端口导致 20029；若仅 3011 可访问则改为 http://localhost:3011
VITE_FEISHU_REDIRECT_BASE=http://localhost:3010
VITE_FEISHU_REDIRECT_URI=/equipment/
# 0=飞书 OAuth 登录（右上角为真实姓名，设备接口走 JWT 鉴权，推荐本地联调）
# 1=开发旁路：固定「开发测试用户」+ dev-bypass-token；须 backend DJANGO_DEBUG=true 后端才认该 token，
#    否则接口会报「请先登录」。旁路下若已配置 DEV_BYPASS_ACCOUNT_ID 指向库内真实账号，可拉到真实 display_name。
VITE_DEV_AUTH_BYPASS=0
