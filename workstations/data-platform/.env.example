VITE_FEISHU_APP_ID=cli_a93753da2c381cef
VITE_API_BASE_URL=/v2/api/v1

# 飞书 OAuth redirect_uri 显式覆盖（优先级最高，本地开发必须填写）
# 生产部署时留空（自动使用 window.location.origin）
# 本地开发时设为生产服务器地址，避免 error 20029（localhost 未在飞书后台注册）
VITE_FEISHU_REDIRECT_BASE=http://118.196.64.48

# 以下两项通常不需要，仅在 redirect_uri 路径有特殊需求时启用
# VITE_FEISHU_REDIRECT_URI=/data-platform/
